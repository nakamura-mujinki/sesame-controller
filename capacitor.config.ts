import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sesame.controller',
  appName: 'Sesame Controller',
  webDir: 'dist'
};

export default config;
